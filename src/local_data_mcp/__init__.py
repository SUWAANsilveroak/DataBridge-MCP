"""Universal Local Data MCP Server.

A Model Context Protocol (MCP) server that exposes safe, validated tools
for interacting with local data sources.

``__version__`` is the single source of truth for the project version. It is
reported by the ``server_info`` tool and read by the packaging config in
``pyproject.toml``.
"""

__version__ = "0.1.0"
