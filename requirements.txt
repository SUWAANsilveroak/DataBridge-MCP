# Runtime dependencies for the Universal Local Data MCP Server.
# Mirrors the [project].dependencies in pyproject.toml, pinned for reproducible
# installs. Dev-only tools (pytest) live in the [dev] optional group.
#
# Install runtime only:      pip install -r requirements.txt
# Install with dev tooling:  pip install -e ".[dev]"

mcp==1.28.1
pydantic==2.13.4
google-auth==2.56.2
google-auth-oauthlib==1.4.0
